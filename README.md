# Grievance Redressal Survey System

Local Vite + React + TypeScript app using Tailwind CSS.

Run locally:

```bash
npm install
npm run dev
```

Features:
- Dark SaaS-style dashboard with glassmorphism
- Survey form with validation
- System status card (online/offline + sync)
- Submitted grievances list stored in localStorage
